import { ScrollShrinkDirective } from './scroll-shrink.directive';

describe('ScrollShrinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollShrinkDirective();
    expect(directive).toBeTruthy();
  });
});
